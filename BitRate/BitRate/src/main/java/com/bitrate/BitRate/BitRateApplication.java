package com.bitrate.BitRate;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;


@SpringBootApplication(scanBasePackages = "com.bitrate.BitRate")
public class BitRateApplication {
    public static void main(String[] args) {
        SpringApplication.run(BitRateApplication.class, args);
    }
}